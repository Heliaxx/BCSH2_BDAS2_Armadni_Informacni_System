<Page x:Class="BCSH2_BDAS2_Armadni_Informacni_System.VojakView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BCSH2_BDAS2_Armadni_Informacni_System" xmlns:viewmodels="clr-namespace:BCSH2_BDAS2_Armadni_Informacni_System.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:VojakViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="VojakView">

    <Grid>
        <DataGrid ItemsSource="{Binding Vojaci}" SelectedItem="{Binding SelectedVojak}" AutoGenerateColumns="True" Height="250"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <Button Content="Načíst" Command="{Binding LoadCommand}" Width="75" Margin="5"/>
            <Button Content="Přidat" Command="{Binding AddCommand}" Width="75" Margin="5"/>
            <Button Content="Aktualizovat" Command="{Binding UpdateCommand}" Width="100" Margin="5"/>
            <Button Content="Smazat" Command="{Binding DeleteCommand}" Width="75" Margin="5"/>
        </StackPanel>
    </Grid>
</Page>
